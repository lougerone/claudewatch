# ClaudeWatch Configuration

# Proxy server port
PORT=3001

# Node environment
NODE_ENV=development

# SQLite database location
DATABASE_URL="file:./claudewatch.db"

# Encryption key for API key storage (REQUIRED - generate a secure key)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=change-me-to-a-64-char-hex-string-run-the-command-above!!!!!!!!

# Dashboard URL for CORS
CORS_ORIGIN=http://localhost:3000
